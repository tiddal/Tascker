<% include ../partials/Header %>
<div class="jumbotron jumbo-bg shadow">
	<h1 class="display-4">Bem-vindo ao Tascker!</h1>
	<p class="lead text-muted">
		Uma ferramenta que te premite criar projetos e adicionar-lhes todas as suas
		respetivas tarefas para que tenhas tudo organizado e planeado!
	</p>
	<hr class="my-4" />
	<p class="text-muted">
		Começa já a pôr as tuas ideias em prática! Clica no botão a baixo para
		iniciares um projeto. Utiliza o teu PIN para partilhares o teu projeto com
		outras pessoas da tua confiança!
	</p>
	<a
		class="btn btn-primary jumbo-button mt-4"
		href="/projects/new"
		role="button"
		><span class="mr-3"><i class="fas fa-file shadow"></i></span>
		<span class="mr-3">Começar um novo projeto</span>
	</a>
</div>
<div class="row mb-5">
	<% projects.forEach((project) => { %>

	<div class="col-12 col-lg-6 my-3 lst">
		<div class="card h-100 lst-card shadow">
			<span class="icon text-center"><i class="fas fa-layer-group"></i></span>
			<div class="card-body lst-card-body">
				<h5 class="card-title">
					<%= project.name %> <% if(project.finishedAt !== null){ %>
					<span class="badge badge-pill badge-secondary float-right"
						>Concluído</span
					>
					<%};%>
				</h5>

				<h6 class="card-subtitle mb-2 text-muted">
					<%= project.author %> - <% if(project.finishedAt !== null){ %>
					concluído <%= moment(project.finishedAt).fromNow()%> <%}else{%>
					iniciado <%= moment(project.createdAt).fromNow() %><%};%><small
						class="float-right mx-1"
						title="Tarefas"
						><%= project.tasks.length %> <i class=" fas fa-tasks "></i
					></small>
					%><small class="float-right mx-1" title="Tempo de trabalho"
						><%= Math.floor(moment.duration(project.duration).asMinutes()) %>'
						<i class=" far fa-clock "></i
					></small>
				</h6>
				<hr />
				<p class="card-text">
					<%= project.description %>
				</p>
				<div class="text-center">
					<a
						href="/projects/<%= project.id %>"
						class="btn btn-primary d-block lst-card-button"
						>Ver Projeto</a
					>
				</div>
			</div>
		</div>
	</div>
	<% }) %>
</div>

<% include ../partials/Footer %>
